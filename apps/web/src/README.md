# Web Source

여기는 실제 프런트엔드 구현 코드가 들어갈 자리다.

지금은 요청에 따라 비워둔 상태다.

권장 시작 파일:

- `main.tsx`
- `App.tsx`
- `styles.css`
- 필요하면 `components/`, `features/`, `lib/`

권장 원칙:

- 산타기 관전형 게임 흐름을 우선한다.
- 로비, 레이스 HUD, 결과 화면을 분리한다.
- API 계약 타입은 UI 상태 타입과 가깝게 둔다.
