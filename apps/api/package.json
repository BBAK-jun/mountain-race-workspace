{
  "name": "@mountain-race/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "files": [],
  "scripts": {
    "dev": "node --eval \"console.log('apps/api is intentionally empty. Read apps/api/README.md before adding code.')\"",
    "build": "node --eval \"console.log('apps/api has no implementation yet. Nothing to build.')\"",
    "start": "node --eval \"console.log('apps/api has no runtime yet. Add server code first.')\"",
    "typecheck": "node --eval \"console.log('apps/api has no TypeScript application code yet.')\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
