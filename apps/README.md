# Apps

이 디렉토리는 실제 애플리케이션이 들어갈 자리다.

현재 상태:

- `apps/web`: Cloudflare Pages에 배포할 정적 웹 앱 자리
- `apps/api`: Cloudflare Workers에 배포할 Hono API 앱 자리

지금은 웹은 빈 플레이스홀더이고, API는 최소 Hono Worker 스타터만 있는 초기 상태다. 실제 기능을 만들 때는 각 앱의 `README.md`를 먼저 읽고 구조를 채우면 된다.
