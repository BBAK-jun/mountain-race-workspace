# Cursor

이 디렉토리는 Cursor 전용 설정을 담는다.

구성:

- `rules/`: 항상 적용되거나 범위가 정해진 규칙
- `skills/`: 필요할 때만 불러오는 작업 절차
- `agents/`: 역할이 분리된 서브에이전트
- `hooks.json`: 세션 종료 등 이벤트 훅
- `mcp.json`: 프로젝트 전용 MCP 설정 자리

현재 앱 구현은 비어 있지만, 나중에 코드를 추가할 때 Cursor가 바로 컨텍스트를 이해할 수 있도록 이 디렉토리는 유지한다.
